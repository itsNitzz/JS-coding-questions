// ----------------------------sets--------------------

// const cuisines = new Set([1, 2, 3, 4, 5, 5, 5]);

// ----------------------------Map---------------------

const questions = new Map([
  ["question", "What is the best programming langauge"],
  [1, "JavaScript"],
  [2, "Python"],
  [3, "Java"],
  ["answer", 3],
]);

// Object.entries() gives [[], []] data structurec
// so to convert objects to Map we can do like that new Map(Object.entries())
